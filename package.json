{
  "name": "beat-maker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "parcel serve src/index.html --open",
    "start": "concurrently -r \"npm:serve\" \"npm:type-check-w\"",
    "build": "tsc --noEmit && rm -rf dist && parcel build src/index.html",
    "clean": "rm -rf dist",
    "type-check-w": "tsc --noEmit --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.0.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.32.8",
    "typescript": "^4.1.5"
  }
}
